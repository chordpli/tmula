export default function App() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', maxWidth: '40rem', margin: '4rem auto', padding: '0 1rem' }}>
      <h1>tmula</h1>
      <p>Real-user traffic simulator — control-plane UI (scaffold).</p>
      <p>
        Define virtual users, a behavior graph, and load profiles, then run an
        experiment and read the findings.
      </p>
    </main>
  )
}
