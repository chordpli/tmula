// Package load drives virtual-user concurrency and load profiles (weight,
// ramp, spike, soak) and talks to the system under test through pluggable
// protocol adapters (REST first, gRPC/WS later).
package load
