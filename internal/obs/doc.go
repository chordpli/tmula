// Package obs collects client-side observations (status, latency percentiles,
// timeouts, error rate) and classifies findings (threshold, contract,
// mutation, availability). Server-side metrics integration is opt-in.
package obs
